# -----------------------------------------------------
# ALIASES
# -----------------------------------------------------

# -----------------------------------------------------
# General
# -----------------------------------------------------
alias c='clear'
alias nf='fastfetch'
alias pf='fastfetch'
alias ff='fastfetch'
alias ls='eza -a --icons=always'
alias ll='eza -al --icons=always'
alias lt='eza -a --tree --level=1 --icons=always'
alias shutdown='systemctl poweroff'
alias v='$EDITOR'
alias vim='$EDITOR'
alias ts='~/.config/ml4w/scripts/snapshot.sh'
alias wifi='nmtui'
alias cleanup='~/.config/ml4w/scripts/cleanup.sh'

# -----------------------------------------------------
# ML4W Apps
# -----------------------------------------------------
alias ml4w='flatpak run com.ml4w.welcome'
alias ml4w-setting='flatpak run com.ml4w.settings'
alias ml4w-calendar='flatpak run com.ml4w.calendar'
alias ml4w-hyprland='com.ml4w.hyprland.settings'
alias ml4w-sidebar='flatpak run com.ml4w.sidebar'
alias ml4w-options='ml4w-hyprland-setup -m options'
alias ml4w-sidebar='ags toggle sidebar'
alias ml4w-diagnosis='~/.config/hypr/scripts/diagnosis.sh'
alias ml4w-hyprland-diagnosis='~/.config/hypr/scripts/diagnosis.sh'
alias ml4w-qtile-diagnosis='~/.config/ml4w/qtile/scripts/diagnosis.sh'
alias ml4w-update='~/.config/ml4w/update.sh'

# -----------------------------------------------------
# Window Managers
# -----------------------------------------------------

alias Qtile='startx'
# Hyprland with Hyprland

# -----------------------------------------------------
# Scripts
# -----------------------------------------------------
alias ascii='~/.config/ml4w/scripts/figlet.sh'

# -----------------------------------------------------
# System
# -----------------------------------------------------
alias update-grub='sudo grub-mkconfig -o /boot/grub/grub.cfg'

# -----------------------------------------------------
# Qtile
# -----------------------------------------------------
alias res1='xrandr --output DisplayPort-0 --mode 2560x1440 --rate 120'
alias res2='xrandr --output DisplayPort-0 --mode 1920x1080 --rate 120'
alias setkb='setxkbmap de;echo "Keyboard set back to de."'

# -----------------------------------------------------
# My Apps
# -----------------------------------------------------
# alias boxy='qutebrowser https://boxy-svg.com/app'
alias zen='/opt/zen/zen'
alias zed="env -u WAYLAND_DISPLAY zed"
alias agile='qutebrowser https://agilefingers.com/es/curso'
alias slack='qutebrowser https://app.slack.com/client/T07J68HS0K1/C07K9JMRA64'
alias coursera='qutebrowser https://www.coursera.org/programs/gestion-de-proyectos-google-2024-xdah9'

# -----------------------------------------------------
# Command Shortcuts
# -----------------------------------------------------
alias comandos="glow -p ~/Comandos\ Linux\ y\ Utilidades.md"
alias pinstall="sudo pacman -S"
alias yinstall="yay -S"
alias cnvim="glow -p ~/Atajos\ Nvim.md"
alias ccode="glow -p ~/Atajos\ Code.md"
alias puninstall="sudo pacman -Rns"
alias yuninstall="yay -Rns"
alias newalias="nvim ~/dotfiles/.config/zshrc/25-aliases"
alias quteconfig="nvim ~/.config/qutebrowser/config.py"
alias nvimconfig="nvim ~/.config/nvim/init.lua"
alias q="exit"
alias :q="exit"

# -----------------------------------------------------
# Custom CSS for Code
# -----------------------------------------------------
# VS Code
# alias customcss='sudo chown -R $(whoami) "$(which code)" && sudo chown -R $(whoami) /usr/lib/code'
# Code OSS
alias customcss='sudo chown -R $(whoami) /usr/bin/code && sudo chown -R $(whoami) /usr/lib/code'
